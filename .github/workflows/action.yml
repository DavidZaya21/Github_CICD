name: Github action

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  hello:
    name: hello
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "hello"
